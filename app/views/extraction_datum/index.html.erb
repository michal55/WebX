<%= extends :basic_panel do %>
  <%= extends :basic_panel_heading do %>
    <h1> Extraction <%= @extraction.created_at %> </h1>
    <div class="row">
      <div class="col-md-6">
        <ol class="breadcrumb">
          <li><%= link_to(@project.name, edit_project_path(@project.id))%></li>
          <li><%= link_to(@script.name, edit_project_script_path(@project.id, @script.id))%></li>
        </ol>
      </div>
    </div>
  <% end %>
  <%= extends :basic_panel_body do %>
   <table class="table table-striped table-hover">
      <thead>
      <tr>
        <th> Field </th>
        <th> Value </th>
      </tr>
      </thead>
      <tbody>
        <% @extraction_datum.each do |d| %>
        <tr>
          <td> <%= d.field_name %> </td>
          <td> <%= d.value %> </td>
        </tr>
      <% end%>
      </tbody>
    </table>
  <% end %>
<% end %>
