<% provide :title, t('user.index.title') %>
<div class="container">
  <h1> All users </h1>
  <% @users.each do |user| %>
      <div class="col-md-6">
        <p><%= user.email %></p>
      </div>
  <% end %>
</div>